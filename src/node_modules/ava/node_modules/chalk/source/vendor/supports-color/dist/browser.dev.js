"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

/* eslint-env browser */
var level = function () {
  if (navigator.userAgentData) {
    var brand = navigator.userAgentData.brands.find(function (_ref) {
      var brand = _ref.brand;
      return brand === 'Chromium';
    });

    if (brand && brand.version > 93) {
      return 3;
    }
  }

  if (/\b(Chrome|Chromium)\//.test(navigator.userAgent)) {
    return 1;
  }

  return 0;
}();

var colorSupport = level !== 0 && {
  level: level,
  hasBasic: true,
  has256: level >= 2,
  has16m: level >= 3
};
var supportsColor = {
  stdout: colorSupport,
  stderr: colorSupport
};
var _default = supportsColor;
exports["default"] = _default;