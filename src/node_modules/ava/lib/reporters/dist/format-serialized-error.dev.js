"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = formatSerializedError;

function formatSerializedError(formattedDetails, message) {
  var printMessage = formattedDetails.length === 0 ? Boolean(message) : !formattedDetails[0].label.startsWith(message);

  if (formattedDetails.length === 0) {
    return {
      formatted: null,
      printMessage: printMessage
    };
  }

  var formatted = '';
  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = formattedDetails[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var value = _step.value;
      formatted += "".concat(value.label, "\n\n").concat(value.formatted, "\n\n");
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator["return"] != null) {
        _iterator["return"]();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  return {
    formatted: formatted.trim(),
    printMessage: printMessage
  };
}