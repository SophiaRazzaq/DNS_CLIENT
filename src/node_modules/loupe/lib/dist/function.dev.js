"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = inspectFunction;

var _helpers = require("./helpers.js");

function inspectFunction(func, options) {
  var functionType = func[Symbol.toStringTag] || 'Function';
  var name = func.name;

  if (!name) {
    return options.stylize("[".concat(functionType, "]"), 'special');
  }

  return options.stylize("[".concat(functionType, " ").concat((0, _helpers.truncate)(name, options.truncate - 11), "]"), 'special');
}