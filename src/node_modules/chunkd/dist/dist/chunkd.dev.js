"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

function chunkd(array, index, total) {
  var length = array.length;
  var size = Math.floor(length / total);
  var remainder = length % total;
  var offset = Math.min(index, remainder) + index * size;
  var chunk = size + (index < remainder ? 1 : 0);
  return array.slice(offset, offset + chunk);
}

exports["default"] = chunkd;
module.exports = chunkd;